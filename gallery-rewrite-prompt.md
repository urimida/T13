# 갤러리 화면 완전 재작성 프롬프트

## 목표
태블릿에서 하루종일 방치되어도 멈추거나 렉이 걸리지 않는 완전 최적화된 전시용 인터랙션 화면을 구현합니다. 화면 디자인과 기능은 기존과 동일하게 유지하되, 코드는 완전히 새로 작성합니다.

## 성능 요구사항
1. **장시간 실행 안정성**: 1시간 이상 방치되어도 메모리 누수 없이 안정적으로 동작
2. **프레임 레이트**: 태블릿에서 최소 30fps 유지 (드래그 중에도)
3. **메모리 관리**: 메모리 사용량이 시간이 지나도 증가하지 않음
4. **GC 최소화**: 가비지 컬렉션으로 인한 프레임 드롭 방지
5. **배터리 효율**: 불필요한 연산 최소화

## 화면 구성 (기존과 동일하게 유지)

### 1. 상단 네비게이션 바
- 왼쪽: 캡쳐 버튼
- 중앙: 네비게이션 바 (클릭 시 모달 표시)
- 오른쪽: 워크룸 버튼
- 반응형 스케일 적용

### 2. 검색창
- 네비게이션 바 아래 중앙 배치
- 클릭 시 전체보기로 전환
- 글래스모피즘 스타일

### 3. 시각적 언어 카드 (왼쪽 세로 배치)
- 6개 카드 세로로 배치
- 각 카드 클릭 시 해당 태그를 가진 버블만 필터링
- 선택된 카드는 하이라이트
- 카드에 버블 개수 표시

### 4. 아크 캐러셀 (화면 하단)
- 원의 윗부분만 보이는 형태 (상단 반원)
- 중앙 버블이 가장 크고, 양쪽으로 갈수록 작아짐
- 좌우 스와이프로 버블 탐색
- 중앙 버블에만 캡, 빛 효과, 텍스트 표시
- 버블 아래에 제목과 태그 표시

### 5. 전체 화면 모드
- 버블 클릭 시 전체 화면으로 확대
- 상단에 날씨 바, 조도 바, 뒤로가기 버튼 표시
- 뒤로가기 버튼 클릭 시 일반 모드로 복귀

## 최적화 전략

### 1. 객체 재사용 및 풀링
- 버블 객체, 벡터, 배열 등을 재사용하여 GC 방지
- 임시 객체 생성 최소화
- 배열 길이 조정 시 `length = 0` 대신 재사용

### 2. 이미지 로딩 최적화
- 화면에 보이는 버블 이미지만 로드 (Viewport Culling)
- 이미지 로드 완료 후 스프라이트 캐시에 저장
- 이미지 크기 최적화 (필요한 크기만 로드)
- 이미지 로드 실패 시 재시도 로직 제거 (에러만 로그)

### 3. 렌더링 최적화
- 화면 밖 버블은 렌더링하지 않음
- 스프라이트 캐시 시스템 유지 (버블 크기별로 캐시)
- 배경은 버퍼에 한 번만 그리고 재사용
- 불필요한 그라디언트/효과 계산 최소화

### 4. 애니메이션 최적화
- `requestAnimationFrame` 기반 프레임 제어
- 드래그 중이 아닐 때는 프레임 스킵 가능
- 애니메이션 값은 lerp 대신 더 효율적인 보간 사용
- 불필요한 업데이트 스킵 (변화가 없으면 업데이트 안 함)

### 5. 이벤트 처리 최적화
- 이벤트 리스너는 한 번만 등록
- 이벤트 핸들러 내부에서 불필요한 계산 최소화
- 터치/마우스 이벤트 통합 처리

### 6. 메모리 누수 방지
- 이벤트 리스너 정리
- 타이머/인터벌 정리
- 이미지 참조 정리 (사용하지 않는 이미지 해제)
- 클로저로 인한 메모리 누수 방지

### 7. 계산 최적화
- 반복되는 계산은 캐시
- 거리 계산은 제곱근 대신 제곱 거리 비교
- 삼각함수 계산 최소화 (각도 미리 계산)
- 불필요한 반복문 최소화

## 코드 구조

### 필수 파일
- `gallery/index.html`: HTML 구조 (기존과 동일)
- `gallery/sketch.js`: p5.js 스케치 코드 (완전히 새로 작성)

### 데이터 파일
- `public/assets/data/bubbles.json`: 버블 데이터 (기존 사용)
- `public/assets/bubble-imgs/*.webp`: 버블 이미지들 (기존 사용)
- `public/assets/public-imgs/*.png`: UI 이미지들 (기존 사용)
- `public/assets/fonts/PretendardVariable.ttf`: 폰트 (기존 사용)

## 구현 세부사항

### 1. 버블 시스템
- 버블은 헥사곤 그리드로 배치 (토러스 래핑)
- 아크 캐러셀 모드에서는 원형 배치
- 버블 크기는 중심 거리에 따라 동적으로 변경
- 필터링 시 버블은 페이드아웃/인 효과

### 2. 이미지 관리
- 이미지는 지연 로딩 (화면에 보일 때만 로드)
- 이미지 로드 상태 추적 (로딩 중, 로드 완료, 로드 실패)
- 스프라이트 캐시: 버블 크기별로 미리 렌더링된 그래픽 저장
- 이미지가 로드되면 해당 스프라이트 캐시 무효화 및 재생성

### 3. 아크 캐러셀
- 7개 슬롯에 버블 표시 (중앙 + 좌우 각 3개)
- 드래그로 버블 탐색
- 스냅 기능: 드래그 종료 시 가장 가까운 버블로 정렬
- 관성 효과: 드래그 속도에 따라 관성 적용

### 4. 필터링 시스템
- 시각적 언어 카드 선택 시 해당 태그를 가진 버블만 표시
- 필터링되지 않은 버블은 페이드아웃 후 제거
- 필터링된 버블은 페이드인 후 표시
- 공통 버블은 유지 (부드러운 전환)

### 5. 전체 화면 모드
- 버블 클릭 시 전체 화면으로 확대 애니메이션
- 전체 화면에서는 배경 어둡게, 버블 이미지 확대
- 상단에 정보 바 표시 (날씨, 조도, 뒤로가기)

## 성능 모니터링
- 개발 모드에서만 FPS 표시
- 메모리 사용량 로깅 (개발 모드)
- 프레임 드롭 감지 및 경고

## 주의사항
1. **절대 하지 말아야 할 것들**:
   - 매 프레임마다 새 객체 생성
   - 불필요한 이미지 로드
   - 무한 루프나 재귀 호출
   - 이벤트 리스너 중복 등록
   - 메모리 누수 가능성 있는 코드

2. **반드시 해야 할 것들**:
   - 객체 재사용
   - 화면 밖 요소 렌더링 스킵
   - 계산 결과 캐시
   - 이벤트 리스너 정리
   - 메모리 누수 방지

## 테스트 요구사항
- 태블릿에서 최소 1시간 이상 실행 테스트
- 메모리 사용량 모니터링 (증가하지 않아야 함)
- 프레임 레이트 모니터링 (30fps 이상 유지)
- 다양한 인터랙션 테스트 (드래그, 클릭, 필터링)

## 기존 코드 참고
현재 `gallery/sketch.js` 파일을 참고하여 기능과 디자인을 동일하게 구현하되, 성능 최적화를 최우선으로 완전히 새로 작성합니다.

